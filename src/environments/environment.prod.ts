/* import {config} from 'dotenv';
config();
console.log(process);
if (process.env.ENV_APP == 'production'){
    config({path: '.env.production'});
}
export const environment = {
    production: true,
    API_URL: process.env.API_URL || 'http://localhost:9090/api'
} */